export const getItemsFiltration = (item) => {
	const itemProject = document.querySelectorAll('.item-projects');
	const ItemsFiltration = itemProject.querySelectorAll(element);
	// item.forEach((element) => {
	// 	const ItemsFiltration = document.querySelectorAll(element);
	// 	ItemsFiltration.forEach((el) => {
	// 		console.log(el)
	// 	})
	// })
}
